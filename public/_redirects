
# Netlify _redirects file
# This handles static files first
/feed.xml                       /feed.xml                         200
/sitemap.xml                    /sitemap.xml                      200
/robots.txt                     /robots.txt                       200

# Handle assets directly
/*.js                           /:splat                           200
/*.css                          /:splat                           200
/*.ico                          /:splat                           200
/*.png                          /:splat                           200
/*.svg                          /:splat                           200

# SPA fallback - this will handle ALL React routes automatically
# without manual listing of each route
/*                              /index.html                       200
