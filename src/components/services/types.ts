
export interface ServiceType {
  title: string;
  description: string;
  price: string;
  subtext?: string;
  extraText?: string;
  features: string[];
  cta: string;
  secondaryCta: string;
  calendlyLink?: string;
  learnMoreLink?: string;
}
